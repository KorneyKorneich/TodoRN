import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        fontSize: 35,
        paddingHorizontal: 20,
        paddingTop: 40,
        paddingBottom: 20,
        color: "#000",
        fontWeight: "bold",
    },
});
