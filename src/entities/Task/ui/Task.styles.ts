import { StyleSheet } from "react-native";

export default StyleSheet.create({
    task: {
        width: "100%",
        // justifyContent: "space-between",
        gap: 10,
        alignItems: "center",
        padding: 10,
        flexDirection: "row",
    },
    text: {
        fontSize: 18,
        color: "#111",
    },
});
